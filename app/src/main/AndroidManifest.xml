<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.khubbart.mysbusaappv3">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="com.example.khubbart.mysbusaappv3.GlobalClass"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_foreground_sbusa_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_foreground_sbusa_logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ElementLookupActivity" />
        <!--
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        -->
        <!-- This section defaults to a pre-Main login screen -->

        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity
            android:name=".ProfileActivity"
            android:label="@string/title_activity_profile" />
    </application>

</manifest>